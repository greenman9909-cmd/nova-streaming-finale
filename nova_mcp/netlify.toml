[build]
  command = "echo 'No build step needed for simple functions'"
  functions = "functions"
  publish = "public"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/mcp"
  status = 200
